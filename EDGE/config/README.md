# EDGE/config

Рабочие конфигурационные файлы для EDGE располагаются в корневой директории репозитория (`config/`).

Эталонные/примерные конфиги перенесены в `EDGE/examples/config`. Используйте их как шаблон при развёртывании.
